const title = document.querySelector("title").innerText;
const url = window.location.href;
const icon = `${url}img/favicon.ico `;

console.log({
  title: document.querySelector("title").innerText,
  url: window.location.href,
});
